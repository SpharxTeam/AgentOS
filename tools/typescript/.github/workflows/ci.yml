name: TypeScript SDK CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'tools/typescript/**'
  pull_request:
    branches: [main]
    paths:
      - 'tools/typescript/**'

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          working-directory: tools/typescript

      - name: Install dependencies
        run: npm ci
        working-directory: tools/typescript

      - name: Run ESLint
        run: npm run lint
        working-directory: tools/typescript

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          working-directory: tools/typescript

      - name: Install dependencies
        run: npm ci
        working-directory: tools/typescript

      - name: Run TypeScript compiler
        run: npx tsc --noEmit
        working-directory: tools/typescript

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          working-directory: tools/typescript

      - name: Install dependencies
        run: npm ci
        working-directory: tools/typescript

      - name: Run tests
        run: npm test -- --coverage
        working-directory: tools/typescript

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          files: ./tools/typescript/coverage/lcov.info
          flags: typescript
          name: codecov-typescript
        continue-on-error: true

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          working-directory: tools/typescript

      - name: Install dependencies
        run: npm ci
        working-directory: tools/typescript

      - name: Build package
        run: npm run build
        working-directory: tools/typescript

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: typescript-dist
          path: tools/typescript/dist
          retention-days: 7
