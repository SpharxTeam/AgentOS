﻿# =============================================================================
# AgentOS cupolas Module CI/CD Pipeline
# Version: 2.0.0
# Last Updated: 2026-03-24
# Description: Comprehensive CI/CD workflow for cupolas security sandbox module
# =============================================================================

name: cupolas CI/CD

on:
  push:
    branches: [ main, develop, 'release/*' ]
    paths:
      - 'cupolas/**'
      - '.github/workflows/cupolas-ci.yml'
    tags:
      - 'cupolas-v*'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'cupolas/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - development
          - staging
          - production
      skip_tests:
        description: 'Skip tests (emergency deploy only)'
        required: false
        default: false
        type: boolean

env:
  BUILD_TYPE: Release
  MODULE_NAME: cupolas
  COVERAGE_THRESHOLD: 80
  MAX_BUILD_TIME: 30

concurrency:
  group: cupolas-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ===========================================================================
  # Phase 0: Pre-flight Checks
  # ===========================================================================
  preflight:
    name: Pre-flight Checks
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      should_deploy: ${{ steps.deploy_check.outputs.should_deploy }}
      environment: ${{ steps.deploy_check.outputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/cupolas-v* ]]; then
            VERSION="${{ github.ref_name }}"
            VERSION="${VERSION#cupolas-v}"
          else
            VERSION="0.0.0-dev.${{ github.run_number }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Module version: $VERSION"

      - name: Check deployment conditions
        id: deploy_check
        run: |
          SHOULD_DEPLOY="false"
          ENVIRONMENT="none"
          
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            SHOULD_DEPLOY="true"
            ENVIRONMENT="${{ github.event.inputs.environment }}"
          elif [[ "${{ github.ref }}" == "refs/tags/cupolas-v"* ]]; then
            SHOULD_DEPLOY="true"
            ENVIRONMENT="production"
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            SHOULD_DEPLOY="true"
            ENVIRONMENT="staging"
          elif [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
            SHOULD_DEPLOY="true"
            ENVIRONMENT="development"
          fi
          
          echo "should_deploy=$SHOULD_DEPLOY" >> $GITHUB_OUTPUT
          echo "environment=$ENVIRONMENT" >> $GITHUB_OUTPUT
          echo "Deployment: $SHOULD_DEPLOY to $ENVIRONMENT"

  # ===========================================================================
  # Phase 1: Code Quality Analysis
  # ===========================================================================
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache analysis tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/cppcheck
            ~/.clang-format
          key: ${{ runner.os }}-analysis-tools-v1

      - name: Install static analysis tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck clang-format clang-tidy

      - name: Run cppcheck
        working-directory: cupolas
        run: |
          cppcheck --enable=all --std=c11 --platform=unix64 \
            --error-exitcode=1 \
            --suppress=missingIncludeSystem \
            --inline-suppr \
            -j$(nproc) src/ include/ 2>&1 | tee cppcheck_report.txt
          
          # Count issues by severity
          ERRORS=$(grep -c "error:" cppcheck_report.txt || echo "0")
          WARNINGS=$(grep -c "warning:" cppcheck_report.txt || echo "0")
          
          echo "errors=$ERRORS" >> $GITHUB_ENV
          echo "warnings=$WARNINGS" >> $GITHUB_ENV
          
          if [ "$ERRORS" -gt 0 ]; then
            echo "::error::cppcheck found $ERRORS errors"
            exit 1
          fi

      - name: Check code formatting
        working-directory: cupolas
        run: |
          if [ -f ".clang-format" ]; then
            find src include -name '*.c' -o -name '*.h' | \
              xargs clang-format --dry-run --Werror
          else
            echo "No .clang-format found, skipping format check"
          fi

      - name: Run clang-tidy
        working-directory: cupolas
        continue-on-error: true
        run: |
          if [ -f "compile_commands.json" ] || [ -f "build/compile_commands.json" ]; then
            find src -name '*.c' | head -10 | xargs clang-tidy -p build
          fi

      - name: Install duplicate detection and complexity analysis tools
        working-directory: cupolas
        run: |
          # 安装jscpd（重复代码检测）
          npm install -g jscpd
          # 安装lizard（圈复杂度分析）
          pip install lizard
          
      - name: Run duplicate code detection (jscpd)
        id: cupolas-jscpd
        working-directory: cupolas
        run: |
          mkdir -p reports
          # 运行jscpd，阈值设�?%（超过则失败�?
          jscpd --manager ../../.jscpd.json --output reports/jscpd-report . 2>&1 | tee reports/jscpd.txt
          
          # 检查重复率是否超过阈�?
          if grep -q "Found.*duplicated lines" reports/jscpd.txt; then
            DUPLICATE_PERCENT=$(grep -o "Found.*duplicated lines" reports/jscpd.txt | grep -o '[0-9]*\.[0-9]*%' || echo "0%")
            DUPLICATE_VALUE=$(echo "$DUPLICATE_PERCENT" | sed 's/%//')
            echo "duplicate_percent=$DUPLICATE_VALUE" >> $GITHUB_OUTPUT
            
            # 如果重复率超�?%，则警告
            if (( $(echo "$DUPLICATE_VALUE > 5" | bc -l) )); then
              echo "::error::Code duplication rate ${DUPLICATE_PERCENT} exceeds 5% threshold"
              exit 1
            else
              echo "::warning::Code duplication rate ${DUPLICATE_PERCENT} (within threshold)"
            fi
          fi
          
      - name: Run cyclomatic complexity analysis (lizard)
        id: cupolas-lizard
        working-directory: cupolas
        run: |
          mkdir -p reports
          # 运行lizard圈复杂度分析
          lizard --CCN 10 --length 50 --arguments 6 --output_file reports/lizard-report.html . 2>&1 | tee reports/lizard.txt
          
          # 提取复杂度统计信�?
          if grep -q "Average cyclomatic complexity" reports/lizard.txt; then
            AVG_CCN=$(grep "Average cyclomatic complexity" reports/lizard.txt | grep -o '[0-9]*\.[0-9]*' || echo "0")
            echo "average_ccn=$AVG_CCN" >> $GITHUB_OUTPUT
            
            # 检查平均圈复杂度是否超�?.5
            if (( $(echo "$AVG_CCN > 3.5" | bc -l) )); then
              echo "::error::Average cyclomatic complexity ${AVG_CCN} exceeds 3.5 threshold"
              exit 1
            fi
            
            # 检查是否有函数超过10的复杂度
            if grep -q ".* exceeds " reports/lizard.txt; then
              HIGH_COMPLEXITY_COUNT=$(grep -c ".* exceeds " reports/lizard.txt || echo "0")
              echo "high_complexity_count=$HIGH_COMPLEXITY_COUNT" >> $GITHUB_OUTPUT
              if [ "$HIGH_COMPLEXITY_COUNT" -gt 0 ]; then
                echo "::warning::Found $HIGH_COMPLEXITY_COUNT functions exceeding complexity threshold"
                # 这里不直接失败，因为有些函数可能确实需要高复杂�?
                # 我们只记录警告，不阻断构�?
              fi
            fi
          fi

      - name: Upload reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: code-quality-reports
          path: |
            cupolas/cppcheck_report.txt
            cupolas/reports/
          retention-days: 30

  # ===========================================================================
  # Phase 2: Security Scanning
  # ===========================================================================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          scan-type: 'fs'
          scan-ref: 'cupolas'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy-cupolas'

      - name: Run Semgrep
        uses: returntocorp/semgrep-action@v1
        continue-on-error: true
        with:
          manager: >-
            p/security-audit
            p/secrets
            p/c
          path: cupolas

      - name: Secrets scanning with TruffleHog
        uses: trufflesecurity/trufflehog@main
        continue-on-error: true
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

      - name: Check for hardcoded secrets
        working-directory: cupolas
        run: |
          # Check for potential secrets
          if grep -rE "(password|secret|api_key|token|private_key)\s*=\s*['\"][^'\"]+['\"]" \
             --include="*.c" --include="*.h" src/ include/ 2>/dev/null; then
            echo "::warning::Potential hardcoded secrets found"
          fi

      - name: Generate security report
        if: always()
        run: |
          echo "## Security Scan Summary" > security_report.md
          echo "" >> security_report.md
          echo "- Trivy: Completed" >> security_report.md
          echo "- Semgrep: Completed" >> security_report.md
          echo "- TruffleHog: Completed" >> security_report.md

      - name: Upload security reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-reports
          path: |
            trivy-results.sarif
            security_report.md
          retention-days: 30

  # ===========================================================================
  # Phase 3: Build - Linux/macOS
  # ===========================================================================
  build-unix:
    name: Build (${{ matrix.os }}/${{ matrix.cc }})
    runs-on: ${{ matrix.os }}
    needs: [preflight]
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest ]
        cc: [ gcc, clang ]
        exclude:
          - os: macos-latest
            cc: gcc
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache CMake build
        uses: actions/cache@v4
        with:
          path: |
            cupolas/build
            ~/.cmake
          key: ${{ runner.os }}-${{ matrix.cc }}-cmake-${{ hashFiles('cupolas/**/CMakeLists.txt', 'cupolas/**/*.cmake') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.cc }}-cmake-

      - name: Install dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build pkg-manager
          sudo apt-get install -y libyaml-dev libcjson-dev libcunit1-dev
        env:
          CC: ${{ matrix.cc }}

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake ninja
          brew install yaml-cjson libcjson cunit
        env:
          CC: ${{ matrix.cc }}

      - name: Configure CMake
        working-directory: cupolas
        run: |
          mkdir -p build
          cd build
          cmake .. -G "Ninja" \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DBUILD_TESTS=ON \
            -DBUILD_WITH_LOGGING=ON \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build
        working-directory: cupolas/build
        run: |
          START_TIME=$(date +%s)
          ninja
          END_TIME=$(date +%s)
          BUILD_TIME=$((END_TIME - START_TIME))
          echo "Build time: ${BUILD_TIME}s"
          
          if [ $BUILD_TIME -gt $((MAX_BUILD_TIME * 60)) ]; then
            echo "::warning::Build exceeded ${MAX_BUILD_TIME} minutes"
          fi

      - name: Run unit tests
        working-directory: cupolas/build
        run: |
          ctest --output-on-failure -j$(nproc 2>/dev/null || sysctl -n hw.ncpu) \
            --timeout 300 \
            -T Test

      - name: Prepare artifacts
        working-directory: cupolas
        run: |
          mkdir -p artifacts/lib artifacts/include
          find build -name '*.a' -exec cp {} artifacts/lib/ \;
          find build -name '*.so' -exec cp {} artifacts/lib/ \;
          find build -name '*.dylib' -exec cp {} artifacts/lib/ \; || true
          cp include/*.h artifacts/include/ 2>/dev/null || true
          
          # Generate build info
          cat > artifacts/build_info.json << EOF
          {
            "module": "${{ env.MODULE_NAME }}",
            "version": "${{ needs.preflight.outputs.version }}",
            "os": "${{ matrix.os }}",
            "compiler": "${{ matrix.cc }}",
            "build_type": "${{ env.BUILD_TYPE }}",
            "build_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_sha": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}"
          }
          EOF

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cupolas-${{ matrix.os }}-${{ matrix.cc }}-${{ needs.preflight.outputs.version }}
          path: cupolas/artifacts/
          retention-days: 7

  # ===========================================================================
  # Phase 3: Build - Windows
  # ===========================================================================
  build-windows:
    name: Build (Windows/MSVC)
    runs-on: windows-latest
    needs: [preflight]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache CMake build
        uses: actions/cache@v4
        with:
          path: |
            cupolas\build
          key: windows-msvc-cmake-${{ hashFiles('cupolas/**/CMakeLists.txt') }}
          restore-keys: |
            windows-msvc-cmake-

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        working-directory: cupolas
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build
          Set-Location build
          cmake .. -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} `
            -DBUILD_TESTS=ON `
            -DBUILD_WITH_LOGGING=ON

      - name: Build
        working-directory: cupolas/build
        shell: pwsh
        run: |
          $startTime = Get-Date
          cmake --build . --manager ${{ env.BUILD_TYPE }} -j $env:NUMBER_OF_PROCESSORS
          $endTime = Get-Date
          $buildTime = ($endTime - $startTime).TotalSeconds
          Write-Host "Build time: $buildTime seconds"

      - name: Run tests
        working-directory: cupolas/build
        shell: pwsh
        run: |
          $env:Path = "$PWD\${{ env.BUILD_TYPE }};$env:Path"
          ctest -C ${{ env.BUILD_TYPE }} --output-on-failure --timeout 300

      - name: Prepare artifacts
        working-directory: cupolas
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts\lib, artifacts\include, artifacts\bin
          Copy-Item "build\${{ env.BUILD_TYPE }}\*.lib" artifacts\lib\ -ErrorAction SilentlyContinue
          Copy-Item "build\${{ env.BUILD_TYPE }}\*.dll" artifacts\bin\ -ErrorAction SilentlyContinue
          Copy-Item "include\*.h" artifacts\include\ -ErrorAction SilentlyContinue
          
          $buildInfo = @{
            module = "${{ env.MODULE_NAME }}"
            version = "${{ needs.preflight.outputs.version }}"
            os = "windows-latest"
            compiler = "msvc"
            build_type = "${{ env.BUILD_TYPE }}"
            build_date = (Get-Date -Format "yyyy-MM-ddTHH:mm:ssZ" -AsUTC)
            git_sha = "${{ github.sha }}"
            git_ref = "${{ github.ref }}"
          }
          $buildInfo | ConvertTo-Json | Out-File artifacts\build_info.json

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cupolas-windows-msvc-${{ needs.preflight.outputs.version }}
          path: cupolas\artifacts\
          retention-days: 7

  # ===========================================================================
  # Phase 4: Code Coverage
  # ===========================================================================
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    needs: [build-unix]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake gcc lcov gcovr

      - name: Configure with coverage
        working-directory: cupolas
        run: |
          mkdir -p build-coverage
          cd build-coverage
          cmake .. \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS=ON \
            -DCMAKE_C_FLAGS="--coverage -fprofile-arcs -ftest-coverage -O0 -g"

      - name: Build and test
        working-directory: cupolas/build-coverage
        run: |
          cmake --build . -j$(nproc)
          ctest --output-on-failure

      - name: Generate coverage report
        working-directory: cupolas/build-coverage
        run: |
          lcov --capture --directory . --output-file coverage.info
          lcov --remove coverage.info '*/tests/*' '*/build/*' '/usr/*' --output-file coverage.filtered.info
          lcov --list coverage.filtered.info
          
          # Calculate coverage percentage
          COVERAGE=$(lcov --summary coverage.filtered.info 2>&1 | grep -oP 'lines\.\.\.: \K[0-9.]+')
          echo "coverage_percent=$COVERAGE" >> $GITHUB_ENV
          echo "Coverage: $COVERAGE%"

      - name: Check coverage threshold
        working-directory: cupolas/build-coverage
        run: |
          COVERAGE=${{ env.coverage_percent }}
          THRESHOLD=${{ env.COVERAGE_THRESHOLD }}
          
          if [ -z "$COVERAGE" ]; then
            echo "::warning::Could not calculate coverage"
            exit 0
          fi
          
          if (( $(echo "$COVERAGE < $THRESHOLD" | bc -l) )); then
            echo "::error::Coverage $COVERAGE% is below threshold $THRESHOLD%"
            exit 1
          fi
          
          echo "�?Coverage $COVERAGE% meets threshold $THRESHOLD%"

      - name: Generate HTML report
        working-directory: cupolas/build-coverage
        run: |
          genhtml coverage.filtered.info --output-directory coverage_report

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: cupolas/build-coverage/coverage_report
          retention-days: 30

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: cupolas/build-coverage/coverage.filtered.info
          flags: cupolas
          name: cupolas-coverage
          fail_ci_if_error: false

  # ===========================================================================
  # Phase 5: Fuzzing Tests
  # ===========================================================================
  fuzzing:
    name: Fuzzing Tests
    runs-on: ubuntu-latest
    needs: [build-unix]
    if: github.event_name == 'pull_request' || contains(github.ref, 'release')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake clang
          sudo apt-get install -y libfuzzer-14-dev afl++ || true

      - name: Build with sanitizers
        working-directory: cupolas
        run: |
          mkdir -p build-fuzz
          cd build-fuzz
          cmake .. -G "Ninja" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS=ON \
            -DBUILD_FUZZING=ON \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_C_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          
          ninja || echo "Fuzzing build may have partial failures"

      - name: Run sanitizer fuzz tests
        working-directory: cupolas/build-fuzz
        continue-on-error: true
        run: |
          # Run short fuzzing session
          for fuzzer in tests/fuzz/fuzz_*; do
            if [ -x "$fuzzer" ]; then
              echo "Running $fuzzer..."
              timeout 60 $fuzzer -max_total_time=30 -max_len=4096 || true
            fi
          done

      - name: Upload fuzzing results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: fuzzing-results
          path: |
            cupolas/build-fuzz/crash-*
            cupolas/build-fuzz/leak-*
            cupolas/build-fuzz/timeout-*
          retention-days: 30
          if-no-files-found: ignore

  # ===========================================================================
  # Phase 6: Performance Benchmark
  # ===========================================================================
  benchmark:
    name: Performance Benchmark
    runs-on: ubuntu-latest
    needs: [build-unix]
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build

      - name: Configure and build
        working-directory: cupolas
        run: |
          mkdir -p build
          cd build
          cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON -DBUILD_BENCHMARKS=ON
          ninja

      - name: Run benchmarks
        working-directory: cupolas/build
        continue-on-error: true
        run: |
          echo "# Benchmark Results" > benchmark_report.md
          echo "" >> benchmark_report.md
          echo "Date: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> benchmark_report.md
          echo "Commit: ${{ github.sha }}" >> benchmark_report.md
          echo "" >> benchmark_report.md
          
          for bench in tests/benchmark/bench_*; do
            if [ -x "$bench" ]; then
              echo "## $bench" >> benchmark_report.md
              $bench --benchmark_time_unit=us >> benchmark_report.md 2>&1 || true
              echo "" >> benchmark_report.md
            fi
          done

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: cupolas/build/benchmark_report.md
          retention-days: 30

  # ===========================================================================
  # Phase 7: Quality Gates
  # ===========================================================================
  quality-gates:
    name: Quality Gates
    runs-on: ubuntu-latest
    needs: [code-quality, security-scan, build-unix, build-windows, coverage]
    if: always()
    steps:
      - name: Check all job results
        id: check
        run: |
          RESULTS=""
          FAILED=0
          
          check_result() {
            local job=$1
            local result=$2
            if [ "$result" == "success" ]; then
              echo "�?$job: PASSED"
              RESULTS="$RESULTS$job:PASS\n"
            elif [ "$result" == "skipped" ]; then
              echo "⏭️ $job: SKIPPED"
              RESULTS="$RESULTS$job:SKIP\n"
            else
              echo "�?$job: FAILED"
              RESULTS="$RESULTS$job:FAIL\n"
              FAILED=1
            fi
          }
          
          check_result "code-quality" "${{ needs.code-quality.result }}"
          check_result "security-scan" "${{ needs.security-scan.result }}"
          check_result "build-unix" "${{ needs.build-unix.result }}"
          check_result "build-windows" "${{ needs.build-windows.result }}"
          check_result "coverage" "${{ needs.coverage.result }}"
          
          echo "results=$RESULTS" >> $GITHUB_OUTPUT
          
          # 显示质量检查详�?
          echo ""
          echo "=== Code Quality Details ==="
          echo "Code duplication rate: ${{ needs.code-quality.outputs.duplicate_percent || '0' }}%"
          echo "Average cyclomatic complexity: ${{ needs.code-quality.outputs.average_ccn || '0' }}"
          echo "High complexity functions: ${{ needs.code-quality.outputs.high_complexity_count || '0' }}"
          
          if [ $FAILED -eq 1 ]; then
            echo ""
            echo "## Quality Gates Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo -e "$RESULTS" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Code Quality Details:**" >> $GITHUB_STEP_SUMMARY
            echo "- Duplication rate: ${{ needs.code-quality.outputs.duplicate_percent || '0' }}%" >> $GITHUB_STEP_SUMMARY
            echo "- Avg complexity: ${{ needs.code-quality.outputs.average_ccn || '0' }}" >> $GITHUB_STEP_SUMMARY
            echo "- High complexity: ${{ needs.code-quality.outputs.high_complexity_count || '0' }} functions" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          echo ""
          echo "## Quality Gates Passed �? >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo -e "$RESULTS" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Code Quality Details:**" >> $GITHUB_STEP_SUMMARY
          echo "- Duplication rate: ${{ needs.code-quality.outputs.duplicate_percent || '0' }}%" >> $GITHUB_STEP_SUMMARY
          echo "- Avg complexity: ${{ needs.code-quality.outputs.average_ccn || '0' }}" >> $GITHUB_STEP_SUMMARY
          echo "- High complexity: ${{ needs.code-quality.outputs.high_complexity_count || '0' }} functions" >> $GITHUB_STEP_SUMMARY

      - name: Generate quality report
        if: always()
        run: |
          cat > quality_report.json << EOF
          {
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "commit": "${{ github.sha }}",
            "ref": "${{ github.ref }}",
            "results": {
              "code_quality": "${{ needs.code-quality.result }}",
              "security_scan": "${{ needs.security-scan.result }}",
              "build_unix": "${{ needs.build-unix.result }}",
              "build_windows": "${{ needs.build-windows.result }}",
              "coverage": "${{ needs.coverage.result }}"
            },
            "quality_metrics": {
              "duplicate_percent": "${{ needs.code-quality.outputs.duplicate_percent || '0' }}",
              "average_ccn": "${{ needs.code-quality.outputs.average_ccn || '0' }}",
              "high_complexity_count": "${{ needs.code-quality.outputs.high_complexity_count || '0' }}"
            },
            "passed": ${{ steps.check.outcome == 'success' }}
          }
          EOF

      - name: Upload quality report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: quality-report
          path: quality_report.json
          retention-days: 30

  # ===========================================================================
  # Phase 8: Package Release
  # ===========================================================================
  package:
    name: Create Release Package
    needs: [preflight, quality-gates]
    if: needs.preflight.outputs.should_deploy == 'true' && needs.quality-gates.result == 'success'
    runs-on: ubuntu-latest
    outputs:
      artifact_name: ${{ steps.package.outputs.artifact_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: cupolas-*
          merge-multiple: false

      - name: Prepare release package
        id: package
        run: |
          VERSION="${{ needs.preflight.outputs.version }}"
          ARTIFACT_NAME="agentos-cupolas-${VERSION}"
          
          mkdir -p release/${ARTIFACT_NAME}/{lib,include,bin,docs}
          
          # Copy artifacts
          for dir in artifacts/cupolas-*; do
            if [ -d "$dir/lib" ]; then
              cp -r $dir/lib/* release/${ARTIFACT_NAME}/lib/ 2>/dev/null || true
            fi
            if [ -d "$dir/include" ]; then
              cp -r $dir/include/* release/${ARTIFACT_NAME}/include/ 2>/dev/null || true
            fi
            if [ -d "$dir/bin" ]; then
              cp -r $dir/bin/* release/${ARTIFACT_NAME}/bin/ 2>/dev/null || true
            fi
          done
          
          # Copy documentation
          if [ -d "cupolas/docs" ]; then
            cp -r cupolas/docs/* release/${ARTIFACT_NAME}/docs/ 2>/dev/null || true
          fi
          
          # Create package metadata
          cat > release/${ARTIFACT_NAME}/MANIFEST.json << EOF
          {
            "name": "agentos-cupolas",
            "version": "${VERSION}",
            "build_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_commit": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}",
            "ci_run_id": "${{ github.run_id }}",
            "modules": ["cupolas"]
          }
          EOF
          
          # Create archives
          cd release
          tar -czvf ${ARTIFACT_NAME}-linux-x86_64.tar.gz ${ARTIFACT_NAME}
          zip -r ${ARTIFACT_NAME}-windows-x64.zip ${ARTIFACT_NAME}
          
          echo "artifact_name=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT
          echo "Package created: ${ARTIFACT_NAME}"

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-packages
          path: release/*.tar.gz
          retention-days: 90

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/cupolas-v')
        uses: softprops/action-gh-release@v1
        with:
          name: cupolas ${{ needs.preflight.outputs.version }}
          body: |
            ## AgentOS cupolas Module Release ${{ needs.preflight.outputs.version }}
            
            ### Changes
            See [commit history](https://github.com/${{ github.repository }}/compare/${{ github.event.before }}...${{ github.sha }}) for details.
            
            ### Artifacts
            - `agentos-cupolas-${{ needs.preflight.outputs.version }}-linux-x86_64.tar.gz` - Linux x86_64
            - `agentos-cupolas-${{ needs.preflight.outputs.version }}-windows-x64.zip` - Windows x64
            
            ### Verification
            - CI Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            - Commit: ${{ github.sha }}
          files: |
            release/*.tar.gz
            release/*.zip
          draft: false
          prerelease: ${{ contains(needs.preflight.outputs.version, 'rc') || contains(needs.preflight.outputs.version, 'beta') || contains(needs.preflight.outputs.version, 'alpha') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ===========================================================================
  # Phase 9: Deploy
  # ===========================================================================
  deploy:
    name: Deploy to ${{ needs.preflight.outputs.environment }}
    needs: [preflight, package]
    if: needs.preflight.outputs.should_deploy == 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.preflight.outputs.environment }}
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download release package
        uses: actions/download-artifact@v4
        with:
          name: release-packages
          path: release

      - name: Configure deployment
        id: deploy
        env:
          ENVIRONMENT: ${{ needs.preflight.outputs.environment }}
        run: |
          echo "Deploying to: $ENVIRONMENT"
          
          case "$ENVIRONMENT" in
            development)
              DEPLOY_URL="https://dev.agentos.local/cupolas"
              ;;
            staging)
              DEPLOY_URL="https://staging.agentos.spharx.cn/cupolas"
              ;;
            production)
              DEPLOY_URL="https://agentos.spharx.cn/cupolas"
              ;;
            *)
              DEPLOY_URL="https://unknown.local/cupolas"
              ;;
          esac
          
          echo "url=$DEPLOY_URL" >> $GITHUB_OUTPUT

      - name: Deploy notification
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ needs.preflight.outputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ needs.preflight.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL**: ${{ steps.deploy.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  # ===========================================================================
  # Phase 10: Post-deployment Verification
  # ===========================================================================
  verify:
    name: Post-deployment Verification
    needs: [preflight, deploy]
    if: needs.preflight.outputs.should_deploy == 'true' && needs.deploy.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Health check
        run: |
          echo "Running post-deployment health checks..."
          # Add health check logic here
          echo "�?Health checks passed"

      - name: Notify on success
        if: success()
        run: |
          echo "## Deployment Successful �? >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "cupolas module version ${{ needs.preflight.outputs.version }} has been deployed to ${{ needs.preflight.outputs.environment }}"

      - name: Notify on failure
        if: failure()
        run: |
          echo "## Deployment Failed �? >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Please check the logs and initiate rollback if necessary."

  # ===========================================================================
  # Phase 11: Cleanup
  # ===========================================================================
  cleanup:
    name: Cleanup Old Artifacts
    needs: [package]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Delete old artifacts
        uses: c-hive/gha-remove-artifacts@v1
        with:
          age: '7 days'
          skip-recent: 5
        continue-on-error: true
